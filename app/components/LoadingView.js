import React from 'react';
import { View, ActivityIndicator } from 'react-native';

const LoadingViewContainer = () => (
	<View>
		<ActivityIndicator size="large" />
	</View>
);

export default LoadingViewContainer