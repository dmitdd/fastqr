import React from 'react';
import { View, Text } from 'react-native';

const PermissionsErrorViewContainer = () => (
	<View>
		<Text>Camera permission is not granted :(</Text>
	</View>
);

export default PermissionsErrorViewContainer